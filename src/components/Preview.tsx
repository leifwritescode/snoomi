import { Devvit } from "@devvit/public-api"
import CustomPostComponent = Devvit.CustomPostComponent;

const Preview: CustomPostComponent = ({ }) => {
  return (
    <blocks>
      <text>Entering the Snoomiverse...</text>
    </blocks>
  );
};

export default Preview;
