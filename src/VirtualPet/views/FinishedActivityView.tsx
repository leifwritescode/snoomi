import { Devvit } from "@devvit/public-api";
import { VirtualPetComponent } from "../types/VirtualPetComponent.js";

const FinishedActivityView: VirtualPetComponent = (context) => {
  return (<text>finishedActivityView</text>);
};

export default FinishedActivityView;
